## Authorizations
# Uncomment out the merging for each enviornment that you'd like to include.
# You can also just copy and paste the tree (do not include the "authorizations") to each
# enviornment if you need something different per enviornment.
authorizations: &AUTHORIZATIONS
  group_base: ou=groups,dc=cashnetusa,dc=com
  ## Requires config.ldap_check_group_membership in devise.rb be true
  # Can have multiple values, must match all to be authorized
  required_groups:
    - uid=admins,ou=groups,dc=cashnetusa,dc=com
    - uid=users,ou=groups,dc=cashnetusa,dc=com
  ## Requires config.ldap_check_attributes in devise.rb to be true
  ## Can have multiple attributes and values, must match all to be authorized
  require_attribute:
    objectClass: inetOrgPerson
    authorizationRole: postsAdmin

## Enviornments

development:
  host: ldap.cashnetusa.com
  port: 389
  attribute: uid
  base: ou=people,dc=cashnetusa,dc=com
  admin_user: cn=admin,dc=cashnetusa,dc=com
  admin_password: admin_password
  ssl: false
  # <<: *AUTHORIZATIONS

test:
  host: ldap.cashnetusa.com
  port: 389
  attribute: uid
  base: ou=people,dc=cashnetusa,dc=com
  admin_user: cn=admin,dc=cashnetusa,dc=com
  admin_password: admin_password
  ssl: false
  # <<: *AUTHORIZATIONS

production:
  host: ldap.cashnetusa.com
  port: 389
  attribute: uid
  base: ou=people,dc=cashnetusa,dc=com
  admin_user: cn=admin,dc=cashnetusa,dc=com
  admin_password: admin_password
  ssl: false
  # <<: *AUTHORIZATIONS
